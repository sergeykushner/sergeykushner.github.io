{
  "name": "website",
  "version": "1.0.0",
  "description": "Personal website with Cloudinary integration",
  "main": "src/js/app.js",
  "scripts": {
    "upload-assets": "node src/js/cloudinary-upload.js",
    "build": "rm -rf .github.io && mkdir .github.io && cp -R public/* .github.io/ && cp -R src .github.io/ && cp -R assets .github.io/ && cp package.json package-lock.json .gitignore .github.io/"
  },
  "dependencies": {
    "cloudinary": "^1.40.0",
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "readline-sync": "^1.4.10"
  },
  "author": "Sergey Kushner",
  "license": "MIT"
}
